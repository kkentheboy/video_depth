name: Extract File Path Actions Mixin Temp

on:
  push:
    branches: [refactor/extract-file-path-actions-mixin-20260906]

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Extract file path actions mixin
        shell: bash
        run: |
          python - <<'PY'
          from __future__ import annotations
          import ast
          import builtins
          import textwrap
          from pathlib import Path

          ui_path = Path('app/depth_fusion_ui.py')
          out_path = Path('app/components/file_path_actions_mixin.py')
          source = ui_path.read_text(encoding='utf-8')
          tree = ast.parse(source, filename=str(ui_path))

          mains = [n for n in tree.body if isinstance(n, ast.ClassDef) and n.name == 'MainWindow']
          if len(mains) != 1:
              raise SystemExit(f'expected one MainWindow class, got {len(mains)}')
          main = mains[0]
          targets = ('pick_video', 'pick_output_path', 'open_output_dir')
          methods = {}
          for name in targets:
              found = [n for n in main.body if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef)) and n.name == name]
              if len(found) != 1:
                  raise SystemExit(f'expected one MainWindow.{name}, got {len(found)}')
              methods[name] = found[0]

          import_nodes = []
          binding_to_node = {}
          for node in tree.body:
              if isinstance(node, ast.Import):
                  import_nodes.append(node)
                  for alias in node.names:
                      bound = alias.asname or alias.name.split('.')[0]
                      binding_to_node.setdefault(bound, node)
              elif isinstance(node, ast.ImportFrom) and node.module != '__future__':
                  import_nodes.append(node)
                  for alias in node.names:
                      if alias.name == '*':
                          raise SystemExit('star imports are not supported')
                      bound = alias.asname or alias.name
                      binding_to_node.setdefault(bound, node)

          builtin_names = set(dir(builtins))
          required_globals = set()
          for name, method in methods.items():
              for call in (x for x in ast.walk(method) if isinstance(x, ast.Call)):
                  if isinstance(call.func, ast.Name) and call.func.id == 'super' and not call.args and not call.keywords:
                      raise SystemExit(f'{name} uses zero-arg super(); refuse mixin extraction')
              loaded, stored, inner_imports = set(), set(), set()
              for x in ast.walk(method):
                  if isinstance(x, ast.Name):
                      (loaded if isinstance(x.ctx, ast.Load) else stored).add(x.id)
                  elif isinstance(x, (ast.FunctionDef, ast.AsyncFunctionDef, ast.Lambda)):
                      if hasattr(x, 'name'):
                          stored.add(x.name)
                      args = x.args
                      stored.update(a.arg for a in args.posonlyargs + args.args + args.kwonlyargs)
                      if args.vararg:
                          stored.add(args.vararg.arg)
                      if args.kwarg:
                          stored.add(args.kwarg.arg)
                  elif isinstance(x, ast.ExceptHandler) and x.name:
                      stored.add(x.name)
                  elif isinstance(x, ast.Import):
                      inner_imports.update((a.asname or a.name.split('.')[0]) for a in x.names)
                  elif isinstance(x, ast.ImportFrom):
                      inner_imports.update((a.asname or a.name) for a in x.names)
              free = loaded - stored - inner_imports - builtin_names
              unresolved = free - set(binding_to_node)
              if unresolved:
                  raise SystemExit(f'{name} has non-import module globals: {sorted(unresolved)}')
              required_globals.update(free)

          needed_import_lines = []
          for node in import_nodes:
              aliases = []
              for alias in node.names:
                  bound = alias.asname or (alias.name.split('.')[0] if isinstance(node, ast.Import) else alias.name)
                  if bound in required_globals and binding_to_node.get(bound) is node:
                      aliases.append(alias)
              if not aliases:
                  continue
              if isinstance(node, ast.Import):
                  rendered = 'import ' + ', '.join(
                      a.name + (f' as {a.asname}' if a.asname else '') for a in aliases
                  )
              else:
                  dots = '.' * node.level
                  rendered = f'from {dots}{node.module or ""} import ' + ', '.join(
                      a.name + (f' as {a.asname}' if a.asname else '') for a in aliases
                  )
              needed_import_lines.append(rendered)

          lines = source.splitlines(keepends=True)
          method_sources = {}
          spans = []
          for name in targets:
              node = methods[name]
              text = ''.join(lines[node.lineno - 1:node.end_lineno])
              method_sources[name] = textwrap.dedent(text).rstrip() + '\n'
              spans.append((node.lineno - 1, node.end_lineno))

          parts = ['# -*- coding: utf-8 -*-\n', 'from __future__ import annotations\n\n']
          if needed_import_lines:
              parts.append('\n'.join(needed_import_lines) + '\n\n')
          parts.append('class FilePathActionsMixin:\n')
          for name in targets:
              parts.append(textwrap.indent(method_sources[name], '    '))
              parts.append('\n')
          mixin_source = ''.join(parts).rstrip() + '\n'
          mixin_tree = ast.parse(mixin_source, filename=str(out_path))
          mixin_class = next(n for n in mixin_tree.body if isinstance(n, ast.ClassDef) and n.name == 'FilePathActionsMixin')
          moved = {
              n.name: n for n in mixin_class.body
              if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef)) and n.name in targets
          }
          for name in targets:
              if name not in moved:
                  raise SystemExit(f'generated mixin missing {name}')
              if ast.dump(methods[name], include_attributes=False) != ast.dump(moved[name], include_attributes=False):
                  raise SystemExit(f'AST mismatch after moving {name}')

          for start, end in sorted(spans, reverse=True):
              del lines[start:end]
          new_ui = ''.join(lines)

          import_line = 'from components.file_path_actions_mixin import FilePathActionsMixin\n'
          if import_line in new_ui:
              raise SystemExit('FilePathActionsMixin import already exists unexpectedly')
          marker = 'from components.resource_management_mixin import ResourceManagementMixin\n'
          if new_ui.count(marker) != 1:
              raise SystemExit('ResourceManagementMixin import marker changed')
          new_ui = new_ui.replace(marker, marker + import_line, 1)

          old_header = 'class MainWindow(ResourceManagementMixin, QMainWindow):'
          new_header = 'class MainWindow(ResourceManagementMixin, FilePathActionsMixin, QMainWindow):'
          if new_ui.count(old_header) != 1:
              raise SystemExit('MainWindow class header changed unexpectedly')
          new_ui = new_ui.replace(old_header, new_header, 1)

          parsed_ui = ast.parse(new_ui, filename=str(ui_path))
          main2 = next(n for n in parsed_ui.body if isinstance(n, ast.ClassDef) and n.name == 'MainWindow')
          remaining = {n.name for n in main2.body if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))}
          leaked = set(targets) & remaining
          if leaked:
              raise SystemExit(f'file path methods still in MainWindow: {sorted(leaked)}')
          base_names = [b.id for b in main2.bases if isinstance(b, ast.Name)]
          required_bases = ['ResourceManagementMixin', 'FilePathActionsMixin', 'QMainWindow']
          if base_names[:3] != required_bases:
              raise SystemExit(f'unexpected MainWindow MRO: {base_names}')
          for n in mixin_tree.body:
              if isinstance(n, ast.ImportFrom) and n.module == 'depth_fusion_ui':
                  raise SystemExit('file path mixin must not import depth_fusion_ui')
              if isinstance(n, ast.Import) and any(a.name == 'depth_fusion_ui' for a in n.names):
                  raise SystemExit('file path mixin must not import depth_fusion_ui')

          out_path.write_text(mixin_source, encoding='utf-8')
          ui_path.write_text(new_ui, encoding='utf-8')
          print('file path actions mixin extraction: PASS')
          print('required globals:', sorted(required_globals))
          PY
          python -m compileall -q app
      - name: Commit extraction and self-delete
        shell: bash
        run: |
          rm .github/workflows/extract-file-path-actions-mixin-temp.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/depth_fusion_ui.py app/components/file_path_actions_mixin.py .github/workflows/extract-file-path-actions-mixin-temp.yml
          git commit -m "refactor: extract file path actions mixin"
          git push
